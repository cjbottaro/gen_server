require 'spec_helper'

describe GenServer do
  it 'has a version number' do
    expect(GenServer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
